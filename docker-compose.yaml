networks:
  default:
    name: faucet

services:
  faucet:
    container_name: faucet
    image: faucet-image:latest
    build:
      context: .
    ports:
      - 3001:8000
    env_file:
      - .env
